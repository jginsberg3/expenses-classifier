[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.128.0",
    "pandas>=2.3.3",
    "python-dotenv>=1.2.1",
    "scikit-learn==1.7.0",
    "uvicorn>=0.40.0",
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]

[tool.pytest.ini_options]
markers = [
    "smoke: marks tests as smoke tests (skipped by default)",
]
